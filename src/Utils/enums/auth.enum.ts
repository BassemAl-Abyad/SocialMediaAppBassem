export enum GenderEnum {
    MALE = "MALE",
    FEMALE = "FEMALE"
}

export enum RoleEnum {
    USER = "USER",
    ADMIN = "ADMIN"
}

export enum SignatureEnum {
    USER = 0,
    ADMIN = 1
}

export enum TokenTypeEnum {
    ACCESS = "ACCESS",
    REFRESH = "REFRESH"
}
